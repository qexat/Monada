include std;

fib: n (int unsigned) -> (n <= 1)
    ? n (int unsigned)
    ! ctx(n-2) + ctx(n-1) (int unsigned);

main: (void) => {
    n (int unsigned) = std::io::read([n]);
    for i (int unsigned) from 0 to n => {
        std::io::println(fib(i) % (std::string));
    }
} -> (void)
